<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yinzhiyu.butterknifedemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LeakApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConstraintActivity" />
        <activity android:name=".diyview.TriangleActivity" />
        <activity android:name=".ShowDiyViewActivity" />

        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${UMENG_CHANNEL_VALUE}" />

        <activity android:name=".IBMSActivity" />
        <activity android:name=".custompop.PopupActivity" />
        <activity android:name=".EasyPopupActivity"></activity>
        <activity android:name=".easypopup.easypop.EasyPopActivity"></activity>
        <activity android:name=".easypopup.basic.BasicActivity"></activity>
        <activity android:name=".easypopup.easypop.RecyclerViewActivity"></activity>
    </application>

</manifest>